<form method="post" action="{{ path('discord_config_delete', {'id': discord_config.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discord_config.id) }}">
    <button class="btn">Delete</button>
</form>
