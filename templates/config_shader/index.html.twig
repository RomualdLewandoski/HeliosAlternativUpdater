{% extends 'base.html.twig' %}

{% block title %}Shader index{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>
            Shaders
            <small>Liste</small>
        </h1>
        <ol class="breadcrumb">
            <li><a href=""><i class="fa fa-dashboard"></i> Home</a></li>
            <li>Shaders</li>
            <li class="active">Liste</li>
        </ol>
    </section>

    <div class="dashboard1">

        <div class="row">
            <div class="col-md-12 text-right mb-4">
                <a href="{{ path('config_shader_new') }}" class="btn btn-sm btn-success"><i class="fa fa-plus"></i>
                    Créer</a>
            </div>
        </div>
        <div class="col-md-12 table-responsive">

            <table class="table" id="myTable">
                <thead>
                <tr>
                    <th>Id</th>

                    <th>Nom</th>
                    <th>Taille</th>
                    <th>Md5</th>
                    <th>actions</th>
                </tr>
                </thead>
                <tbody>
                {% for shader in shaders %}
                    <tr>
                        <td>{{ shader.idName }}</td>
                        <td>{{ shader.name }}</td>
                        <td>{{ shader.size }}</td>
                        <td>{{ shader.md5 }}</td>
                        <td>
                            <a href="{{ path('config_shader_show', {'id': shader.id}) }}"
                               class="btn btn-sm btn-success">Voir</a>
                            <a href="{{ path('config_shader_edit', {'id': shader.id}) }}"
                               class="btn btn-sm btn-warning">Editer</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="9">no records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            <script>
                $(document).ready(() => {
                    $('#myTable').dataTable()
                })
            </script>
        </div>


    </div>
{% endblock %}
